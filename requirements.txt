# ---- Core deep-learning stack ----
torch>=2.1
transformers>=4.40,<4.45
datasets>=2.18
accelerate>=0.29
sentencepiece>=0.2.0
protobuf>=3.20,<4

# ---- Metrics ----
sacrebleu>=2.4.0          # corpus BLEU / chrF (Post, 2018)
jiwer>=3.0.0              # WER / TER
rouge-score>=0.1.2

# ---- Data / utils ----
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
tqdm>=4.66
scikit-learn>=1.3        # stratified splitting helpers
tabulate>=0.9

# ---- Reading the raw corpus (Excel optional) ----
openpyxl>=3.1

# ---- GPU orchestration (optional, only for remote training) ----
modal>=0.64
